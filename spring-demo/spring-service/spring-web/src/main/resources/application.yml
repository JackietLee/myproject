server:
  port: 10001
spring:
  application:
    name: web
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
    sentinel:
      transport:
        port: 8719
        dashboard: localhost:8080
#feign:
#  circuitbreaker:
#    enabled: true

feign:
  sentinel:
    enabled: true
